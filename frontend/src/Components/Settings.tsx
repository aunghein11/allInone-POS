import Layout from "./Layout";

const Settings = () => {
  return (
    <Layout title="Settings">
      <div>
        <h1>Settings page</h1>
      </div>
    </Layout>
  );
};

export default Settings;
